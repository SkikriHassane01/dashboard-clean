/// <reference types="vite/client" />

// Minimal type shims for packages without bundled typings in this setup.
// (Keeps imports working; you can replace with @types/* later if desired.)
declare module 'xlsx';
declare module 'html2canvas';
